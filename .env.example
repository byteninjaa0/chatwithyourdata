# --- LLM (use Azure OR OpenAI, not both required) ---

# Azure OpenAI (recommended if you use Azure)
AZURE_API_KEY=
AZURE_API_BASE=https://your-resource.openai.azure.com
AZURE_API_VERSION=2024-08-01-preview
AZURE_DEPLOYMENT_NAME=gpt-4o

# OpenAI (used when Azure vars are not set)
OPENAI_API_KEY=

# --- Web search (optional but used by the demo) ---
TAVILY_API_KEY=
