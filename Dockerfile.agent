FROM python:3.11-slim

WORKDIR /app

COPY agent/requirements.txt agent/requirements.txt
RUN pip install --no-cache-dir -r agent/requirements.txt

COPY agent/ agent/

ENV PYTHONUNBUFFERED=1
ENV LANGGRAPH_AGENT_PORT=8000

EXPOSE 8000
WORKDIR /app/agent
CMD ["python", "main.py"]
